# 🗑️ Система автоматической очистки неиспользуемых изображений

## Описание

Система автоматически удаляет изображения из `backend/uploads/`, которые больше не используются в базе данных. Это помогает:
- ✅ Экономить место на сервере
- ✅ Избежать накопления мусорных файлов
- ✅ Поддерживать чистоту файловой системы

## 🔧 Автоматическое удаление

### При удалении записей

Когда вы удаляете запись через админ-панель, связанные изображения **автоматически удаляются**:

- **Каталог товаров**: удаляются `image` и все `documents[]`
- **Услуги**: удаляются `image` и `icon`
- **Новости**: удаляется `coverImage`
- **Команда**: удаляется `photo`
- **Партнеры**: удаляется `logo`
- **Бренды**: удаляется `logo`
- **Галерея**: удаляется `image`

### При обновлении изображений

Когда вы загружаете новое изображение вместо старого, **старое автоматически удаляется**.

Например:
1. У товара было изображение `/static/misc/old-image.webp`
2. Вы загружаете новое изображение `/static/misc/new-image.webp`
3. Старое изображение `old-image.webp` **автоматически удаляется**

## 🧹 Ручная очистка (скрипт)

### Когда использовать?

Запускайте скрипт очистки если:
- Были удалены записи из БД напрямую (не через админ-панель)
- Накопились старые неиспользуемые файлы
- Нужно освободить место на сервере
- После миграции данных

### Как запустить?

```bash
# Локально
cd backend
npm run cleanup-images

# В Docker
docker-compose exec app npm run cleanup-images
```

### Что делает скрипт?

1. ✅ Сканирует все файлы в `backend/uploads/`
2. ✅ Проверяет базу данных на использование каждого файла
3. ✅ Находит неиспользуемые файлы
4. ✅ Удаляет их и показывает статистику

### Пример вывода:

```
🔍 Начинаем поиск неиспользуемых изображений...

📂 Сканируем директорию uploads...
   Найдено файлов: 150

🗄️  Проверяем базу данных...
   Используется изображений: 120

📊 Статистика:
   Всего файлов: 150
   Используется: 120
   Не используется: 30

⚠️  ВНИМАНИЕ! Будут удалены следующие файлы:
────────────────────────────────────────────────────────────
   /static/misc/old-image-1.webp
   /static/misc/old-image-2.webp
   ... и еще 28 файлов
────────────────────────────────────────────────────────────

🗑️  Удаляем неиспользуемые файлы...

✅ Deleted file: uploads/misc/old-image-1.webp
✅ Deleted file: uploads/misc/old-image-2.webp
...

✨ Очистка завершена!
   Удалено файлов: 30/30

💾 Освобождено место на диске
```

## 📋 Какие файлы проверяются?

Скрипт проверяет использование изображений в следующих таблицах:

| Таблица | Поля с изображениями |
|---------|---------------------|
| `User` | `avatar` |
| `Brand` | `logo` |
| `CatalogItem` | `image`, `documents[]` |
| `Service` | `image`, `icon` |
| `News` | `coverImage` |
| `TeamMember` | `photo` |
| `Partner` | `logo` |
| `Gallery` | `image` |

## ⚙️ Техническая информация

### Функции утилит

**`/src/utils/file.js`:**

- `deleteFile(fileUrl)` - удаляет один файл по URL
- `deleteFiles(fileUrls)` - удаляет массив файлов
- `getAllUploadedFiles()` - получает список всех файлов в uploads

### Примеры использования в коде

```javascript
import { deleteFile, deleteFiles } from '../utils/file.js';

// Удалить одно изображение
await deleteFile('/static/misc/image.webp');

// Удалить несколько изображений
await deleteFiles([
  '/static/misc/image1.webp',
  '/static/misc/image2.webp'
]);
```

## 🔒 Безопасность

- ✅ Удаляются только файлы начинающиеся с `/static/`
- ✅ Проверка существования файла перед удалением
- ✅ Обработка ошибок при удалении
- ✅ Логирование всех операций

## 📅 Рекомендации по использованию

### Как часто запускать скрипт?

- **При активной работе**: 1 раз в неделю
- **При стабильной работе**: 1 раз в месяц
- **После миграции/импорта**: сразу после завершения
- **При нехватке места**: по необходимости

### Автоматизация (опционально)

Можно добавить в cron на сервере:

```bash
# Запускать каждое воскресенье в 3:00
0 3 * * 0 cd /path/to/backend && npm run cleanup-images
```

## 🚨 Важные заметки

1. **Внешние URL не затрагиваются**: Если изображение хранится по внешнему URL (например, `https://example.com/image.png`), оно не будет удалено.

2. **Backup**: Перед первым запуском рекомендуется сделать backup папки `uploads/`.

3. **Docker**: При использовании Docker, файлы удаляются внутри контейнера, но благодаря volumes изменения сохраняются на хосте.

## 🎯 Результат

После внедрения системы:
- 🎉 Автоматическое управление файлами
- 💾 Экономия дискового пространства
- 🧹 Чистая файловая система
- ⚡ Улучшенная производительность



