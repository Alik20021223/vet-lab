# Скрипты автоматического перевода

Эти скрипты автоматически переводят данные из базы данных с русского на английский язык.

## 📋 Доступные скрипты

### 1. Перевод услуг (Services)
```bash
npm run translate:services
```
Переводит:
- `title` → `titleEn`
- `shortDescription` → `shortDescriptionEn`
- `fullDescription` → `fullDescriptionEn`

### 2. Перевод каталога товаров (Catalog)
```bash
npm run translate:catalog
```
Переводит:
- `title` → `titleEn`
- `description` → `descriptionEn`
- `fullDescription` → `fullDescriptionEn`
- `applicationMethod` → `applicationMethodEn`

### 3. Перевод команды (Team)
```bash
npm run translate:team
```
Переводит:
- `name` → `nameEn`
- `position` → `positionEn`

### 4. Перевод новостей (News)
```bash
npm run translate:news
```
Переводит:
- `title` → `titleEn`
- `excerpt` → `excerptEn`
- `content` → `contentEn`

### 5. Перевод вакансий (Careers)
```bash
npm run translate:careers
```
Переводит:
- `title` → `titleEn`
- `description` → `descriptionEn`
- `fullDescription` → `fullDescriptionEn`
- `location` → `locationEn`
- `department` → `departmentEn`
- `requirements` → `requirementsEn` (массив)
- `responsibilities` → `responsibilitiesEn` (массив)
- `benefits` → `benefitsEn` (массив)

### 6. Перевод контактной информации (Contacts)
```bash
npm run translate:contacts
```
Переводит:
- `address` → `addressEn`
- `workingHours` → `workingHoursEn`

### 7. Перевести всё сразу
```bash
npm run translate:all
```
Запускает все шесть скриптов последовательно.

## ⚙️ Как это работает

1. Скрипты используют бесплатный API Google Translate
2. Между запросами есть задержка 500мс, чтобы не превысить лимиты
3. Если перевод уже существует, запись пропускается
4. Можно запускать многократно - скрипт умный и не перезапишет существующие переводы

## 🚀 Порядок действий

1. Перейдите в папку backend:
```bash
cd backend
```

2. Убедитесь, что база данных настроена и доступна

3. Запустите нужный скрипт:
```bash
npm run translate:services   # Услуги
npm run translate:catalog    # Каталог товаров
npm run translate:team       # Команда
npm run translate:news       # Новости
npm run translate:careers    # Вакансии
npm run translate:contacts   # Контактная информация
npm run translate:all        # Всё сразу
```

4. Скрипт покажет прогресс и результаты:
```
🚀 Начинаем перевод услуг на английский...

📝 Найдено услуг: 5

🔄 Переводим: "Лабораторная диагностика"
✅ Переведено: "Лабораторная диагностика" -> "Laboratory diagnostics"

📊 Результаты:
   Переведено: 5
   Пропущено: 0
   Всего: 5

✨ Готово!
```

## ⚠️ Важные замечания

1. **Проверьте переводы**: Автоматический перевод может быть неточным, особенно для технических терминов. Рекомендуется проверить и при необходимости отредактировать переводы вручную через админ-панель.

2. **Интернет-соединение**: Требуется стабильное подключение к интернету для работы API перевода.

3. **Время выполнения**: В зависимости от количества записей, скрипт может работать несколько минут.

4. **Безопасность**: Скрипты не удаляют и не изменяют существующие русские данные, только добавляют английские переводы.

## 🔧 Ручное редактирование

После автоматического перевода вы можете отредактировать переводы через:
- Админ-панель сайта
- Prisma Studio: `npm run prisma:studio`
- Напрямую в базе данных

## 📝 Примеры использования

### Перевести только новые записи
Просто запустите скрипт - он автоматически пропустит уже переведённые записи.

### Перезаписать существующие переводы
Если нужно обновить уже существующие переводы, вручную удалите английские поля в базе данных или через Prisma Studio, затем запустите скрипт снова.

### Перевести отдельную запись
Используйте Prisma Studio для ручного редактирования конкретной записи.



