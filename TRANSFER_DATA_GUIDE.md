# 📦 Руководство по переносу данных на сервер

Это руководство покажет, как перенести вашу локальную базу данных и загруженные файлы на production сервер.

## 🎯 Что будет перенесено

- ✅ **База данных** - все товары, услуги, новости, команда и т.д.
- ✅ **Загруженные файлы** - все изображения из `backend/uploads/`
- ✅ **Структура БД** - таблицы и связи (через Prisma миграции)

## 📤 Шаг 1: Экспорт данных с локального компьютера

### Автоматический экспорт (рекомендуется):

```bash
# Находясь в корне проекта
bash scripts/export-database.sh
```

Этот скрипт:
- Экспортирует все данные из вашей локальной базы данных
- Создаст архив папки `backend/uploads/`
- Сохранит всё в папку `database-export/`

### Ручной экспорт (если скрипт не работает):

#### Для SQLite:
```bash
cd backend
sqlite3 prisma/dev.db .dump > ../database-export/database_backup.sql
```

#### Для PostgreSQL:
```bash
pg_dump -U your_user -d your_database > database-export/database_backup.sql
```

#### Архивирование uploads:
```bash
tar -czf database-export/uploads.tar.gz backend/uploads/
```

## 🚀 Шаг 2: Копирование на сервер

### Вариант А: Через SCP

```bash
# Скопируйте папку database-export на сервер
scp -r database-export root@your-server-ip:/home/vet-lab/

# Или если вы создали архив:
tar -czf database-export.tar.gz database-export/
scp database-export.tar.gz root@your-server-ip:/home/vet-lab/
```

### Вариант Б: Через Git

```bash
# Добавьте папку database-export в .gitignore временно закомментировав строку:
# Откройте .gitignore и закомментируйте строки с *.sql и *.tar.gz

git add database-export/
git commit -m "Add database export for migration"
git push

# На сервере:
cd /home/vet-lab
git pull
```

**⚠️ Внимание:** После переноса верните изменения в .gitignore и удалите файлы из Git!

### Вариант В: Через SFTP

Используйте любой SFTP клиент (FileZilla, Cyberduck) и скопируйте папку `database-export` на сервер.

## 📥 Шаг 3: Импорт данных на сервере

### Подключитесь к серверу:
```bash
ssh root@your-server-ip
cd /home/vet-lab
```

### Автоматический импорт (рекомендуется):

```bash
bash scripts/import-database.sh
```

### Ручной импорт:

```bash
# 1. Убедитесь, что контейнеры запущены
docker compose -f docker-compose.production.yml up -d

# 2. Подождите, пока база данных запустится
sleep 10

# 3. Импортируйте SQL дамп
docker compose -f docker-compose.production.yml exec -T db \
  psql -U vetlab_user vetlab_db < database-export/database_backup_*.sql

# 4. Восстановите uploads
tar -xzf database-export/uploads_*.tar.gz

# 5. Перезапустите контейнеры
docker compose -f docker-compose.production.yml restart
```

## ✅ Проверка переноса

### 1. Проверьте, что данные в базе:

```bash
# Подключитесь к БД
docker compose -f docker-compose.production.yml exec db psql -U vetlab_user vetlab_db

# В psql выполните:
\dt                           # Список таблиц
SELECT COUNT(*) FROM "User";  # Количество пользователей
SELECT COUNT(*) FROM "Catalog"; # Количество товаров
\q                            # Выход
```

### 2. Проверьте загруженные файлы:

```bash
ls -lh backend/uploads/
```

### 3. Откройте сайт в браузере и проверьте:
- Отображаются ли товары с изображениями
- Доступны ли услуги
- Показываются ли новости
- Отображается ли команда

## 🔄 Альтернатива: Использование Prisma Seed

Если вы хотите программно заполнить базу данных:

### 1. Создайте seed файл:

```bash
# backend/prisma/seed.js
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

async function main() {
  // Создайте администратора
  await prisma.user.create({
    data: {
      username: 'admin',
      email: 'admin@vetlab.com',
      password: 'hashed_password',
      role: 'super_admin',
    },
  });

  // Добавьте товары, услуги и т.д.
  await prisma.catalog.createMany({
    data: [
      // Ваши данные
    ],
  });
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

### 2. Добавьте в package.json:

```json
{
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}
```

### 3. Запустите seed на сервере:

```bash
docker compose -f docker-compose.production.yml exec backend npx prisma db seed
```

## 🗑️ Очистка после переноса

После успешного переноса данных:

```bash
# На локальном компьютере
rm -rf database-export/

# На сервере
rm -rf /home/vet-lab/database-export/
```

## ❓ Часто задаваемые вопросы

### Q: Нужно ли переносить node_modules?
**A:** Нет! Docker соберёт всё автоматически при сборке образов.

### Q: Что если у меня много данных?
**A:** Используйте сжатие и копируйте файлы частями, или используйте rsync для инкрементного копирования.

### Q: Можно ли обновить данные без полного пересоздания?
**A:** Да, импортируйте только новые данные с помощью SQL INSERT запросов.

### Q: Изображения не отображаются после переноса?
**A:** Убедитесь, что:
1. Папка `backend/uploads/` скопирована полностью
2. Права доступа правильные: `chmod -R 755 backend/uploads/`
3. В коде пути к изображениям относительные, без хардкода localhost

### Q: Можно ли использовать rsync вместо scp?
**A:** Да! Это даже лучше для больших файлов:

```bash
rsync -avz --progress database-export/ root@your-server-ip:/home/vet-lab/database-export/
rsync -avz --progress backend/uploads/ root@your-server-ip:/home/vet-lab/backend/uploads/
```

## 📝 Контрольный список переноса данных

- [ ] Экспорт локальной базы данных выполнен
- [ ] Файлы uploads заархивированы
- [ ] Файлы скопированы на сервер
- [ ] Docker контейнеры запущены на сервере
- [ ] Prisma миграции применены
- [ ] SQL дамп импортирован
- [ ] Файлы uploads распакованы
- [ ] Права доступа на файлы проверены
- [ ] Данные отображаются на сайте
- [ ] Изображения загружаются корректно
- [ ] Временные файлы удалены

## 🎉 Готово!

Теперь ваш production сервер содержит все данные из локальной разработки!

---

💡 **Совет:** Регулярно делайте резервные копии production данных с помощью `scripts/backup.sh`
