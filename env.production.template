# Production Environment Variables Template
# Copy this file to .env.production and fill in your values

# ===========================================
# DATABASE
# ===========================================
DB_PASSWORD=your-strong-database-password-here

# ===========================================
# JWT SECRETS (Generate strong random strings!)
# Use: openssl rand -base64 32
# ===========================================
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long
REFRESH_SECRET=your-super-secret-refresh-key-minimum-32-characters-long

# ===========================================
# CORS ORIGIN
# ===========================================
# Your domain or IP address
CORS_ORIGIN=https://yourdomain.com
# For multiple origins, use comma-separated:
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# ===========================================
# FRONTEND API URL
# ===========================================
# URL where frontend will connect to backend API
# 
# ⚠️ ВАЖНО: Vite встраивает переменные во время СБОРКИ, не во время выполнения!
# Установите VITE_API_URL ПЕРЕД запуском: npm run build
#
# Рекомендуется использовать относительный путь для production (same domain):
VITE_API_URL=/api
#
# Или полный URL (если API на другом домене):
# VITE_API_URL=https://api.yourdomain.com/api
#
# Для разработки (локально):
# VITE_API_URL=http://localhost:4000/api
#
# ⚠️ ВАЖНО: После изменения пересоберите фронтенд!
# bash scripts/build-and-deploy.sh
# или
# cd frontend && npm run build

# ===========================================
# PUBLIC URL (для изображений и статических файлов)
# ===========================================
# Базовый URL для доступа к статическим файлам (изображениям)
# 
# BACKEND (PUBLIC_URL):
# Используется бэкендом для формирования полных URL изображений в API ответах
# Примеры:
# PUBLIC_URL=https://yourdomain.com
# PUBLIC_URL=http://localhost:4000
# 
# Если не указан, бэкенд будет возвращать относительные пути (/static/...)
# 
# FRONTEND (VITE_PUBLIC_URL):
# Используется фронтендом для разрешения относительных путей к изображениям
# Должен совпадать с PUBLIC_URL бэкенда
# Примеры:
# VITE_PUBLIC_URL=https://yourdomain.com
# VITE_PUBLIC_URL=http://localhost:4000
# 
# ⚠️ ВАЖНО: После изменения пересоберите фронтенд!
# bash scripts/build-and-deploy.sh
PUBLIC_URL=
VITE_PUBLIC_URL=

# ===========================================
# NOTIFICATIONS (Contact Form)
# ===========================================

# --- Telegram Bot ---
# Get from @BotFather in Telegram
# 1. Open Telegram, find @BotFather
# 2. Send: /newbot
# 3. Follow instructions
# 4. Copy the TOKEN
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Your Chat ID (where messages will be sent)
# Get from: https://api.telegram.org/botYOUR_TOKEN/getUpdates
# Send a message to your bot first, then check the URL
TELEGRAM_CHAT_ID=123456789


