# 📋 Обзор проекта VetLab

## 🎯 Что это за проект?

**VET-LAB** - это полнофункциональное веб-приложение для ветеринарной лаборатории.

### Основной функционал:
- 📦 **Каталог продукции** - вакцины, препараты, оборудование
- 🔬 **Услуги лаборатории** - описание диагностических услуг
- 📰 **Новости и статьи** - информационный раздел
- 👥 **Команда** - информация о сотрудниках
- 💼 **Карьера** - вакансии
- 📞 **Контактная форма** - обратная связь с уведомлениями в Telegram
- 🔐 **Админ-панель** - управление контентом (требует авторизации)

## 🏗️ Технологический стек

### Frontend:
- **React** + **TypeScript** - UI фреймворк
- **Vite** - сборщик и dev server
- **React Router** - маршрутизация
- **Tailwind CSS** - стилизация
- **Production Build** - статические файлы в `dist/` (НЕ dev server!)

### Backend:
- **Fastify** - веб-сервер (Node.js)
- **Prisma** - ORM для работы с базой данных
- **PostgreSQL** - база данных
- **JWT** - аутентификация
- **Telegram Bot API** - уведомления

### Инфраструктура:
- **Docker** + **Docker Compose** - контейнеризация
- **Nginx** - обратный прокси и статические файлы
- **PostgreSQL** - база данных в Docker контейнере

## 📁 Структура проекта

```
vet-lab/
├── frontend/          # React приложение
│   ├── dist/          # ⚠️ Production build (собирается локально)
│   ├── src/           # Исходный код
│   ├── package.json
│   └── Dockerfile.production  # Docker для production (использует dist/)
│
├── backend/           # Fastify API
│   ├── src/           # Исходный код
│   ├── prisma/        # Схема базы данных
│   ├── uploads/       # Загруженные файлы (фото)
│   └── docker/        # Docker конфигурация
│
├── docker-compose.production.yml  # Docker Compose для production
├── .env.production    # Переменные окружения (создать из шаблона)
└── scripts/           # Скрипты автоматизации
    ├── deploy.sh      # Деплой на сервер
    └── build-and-deploy.sh  # Сборка фронтенда + деплой
```

## 🚀 Запуск на сервере

### ⚠️ ВАЖНО: Frontend работает через Production Build!

**Frontend НЕ запускается как dev server!** 

Вместо этого:
1. **Локально** собирается production build: `npm run build` → создается папка `dist/`
2. **На сервере** Docker копирует `dist/` в Nginx контейнер
3. **Nginx** отдает статические файлы из `dist/`

### Процесс деплоя:

#### 1. Локально (на вашем компьютере):

```bash
# Соберите фронтенд
cd frontend
npm install
npm run build  # Создаст папку dist/

# Вернитесь в корень
cd ..
```

#### 2. На сервере:

```bash
# 1. Клонируйте проект (или обновите)
cd /var/www/vet-lab
git pull

# 2. Убедитесь что frontend/dist/ существует
ls -la frontend/dist/

# 3. Создайте .env.production (если еще нет)
cp env.production.template .env.production
nano .env.production  # Заполните переменные

# 4. Запустите деплой
bash scripts/deploy.sh
```

### Что делает `deploy.sh`:

1. ✅ Проверяет наличие `frontend/dist/`
2. ✅ Загружает переменные из `.env.production`
3. ✅ Останавливает старые контейнеры
4. ✅ Собирает Docker образы:
   - **Backend** - устанавливает зависимости, генерирует Prisma Client
   - **Frontend** - копирует `dist/` в Nginx контейнер
   - **PostgreSQL** - база данных
5. ✅ Запускает контейнеры
6. ✅ Ждет готовности базы данных
7. ✅ Применяет миграции Prisma

## 🐳 Docker контейнеры

После запуска `deploy.sh` будут запущены:

1. **vetlab-db** (PostgreSQL)
   - Порт: `5432` (внутри Docker сети)
   - Данные: volume `postgres_data`

2. **vetlab-backend** (Fastify API)
   - Порт: `3000` (внутри Docker сети)
   - Доступен через Nginx на `/api`

3. **vetlab-frontend** (Nginx + React Build)
   - Порт: `8080` (на хосте)
   - Содержит статические файлы из `frontend/dist/`
   - Доступен через Nginx на `/`

## 🌐 Доступ к приложению

### Через Nginx (рекомендуется):

После настройки Nginx на сервере:
- **Frontend**: `http://yourdomain.com` или `http://your-ip`
- **Backend API**: `http://yourdomain.com/api`

### Напрямую (для тестирования):

- **Frontend**: `http://your-ip:8080`
- **Backend**: `http://your-ip:3000`

## 📝 Переменные окружения

Создайте `.env.production` из шаблона:

```bash
cp env.production.template .env.production
```

**Обязательные переменные:**
- `DB_PASSWORD` - пароль для PostgreSQL
- `JWT_SECRET` - секрет для JWT токенов
- `REFRESH_SECRET` - секрет для refresh токенов
- `VITE_API_URL` - URL API для фронтенда (например: `/api` или `http://yourdomain.com/api`)

## 🔧 Основные команды

### На сервере:

```bash
# Деплой/обновление
bash scripts/deploy.sh

# Просмотр логов
docker compose --env-file .env.production -f docker-compose.production.yml logs -f

# Остановка
docker compose --env-file .env.production -f docker-compose.production.yml down

# Перезапуск
docker compose --env-file .env.production -f docker-compose.production.yml restart

# Создание администратора
docker compose --env-file .env.production -f docker-compose.production.yml exec backend npm run create-admin
```

### Локально (для разработки):

```bash
# Frontend
cd frontend
npm install
npm run dev        # Dev server (localhost:5173)
npm run build      # Production build → dist/

# Backend
cd backend
npm install
npm run dev        # Dev server (localhost:3000)
```

## ⚠️ Важные моменты

### 1. Frontend = Production Build

- ❌ **НЕ** запускается `npm run dev` на сервере
- ✅ Используется **собранный** `dist/` через Nginx
- ✅ Сборка происходит **локально** перед деплоем

### 2. Prisma на сервере

- ✅ Prisma Client генерируется **автоматически** при сборке Docker образа
- ✅ Node.js **НЕ нужен** на сервере (всё в Docker)
- ✅ Миграции применяются через `docker exec`

### 3. База данных

- ✅ PostgreSQL в Docker контейнере
- ✅ Данные сохраняются в Docker volume
- ✅ Миграции применяются автоматически при деплое

### 4. Файлы загрузок

- ✅ Хранятся в `backend/uploads/` на хосте
- ✅ Монтируются в контейнер как volume
- ✅ Доступны через Nginx на `/static`

## 📚 Дополнительная документация

- **`DEPLOYMENT_GUIDE.md`** - подробное руководство по развертыванию
- **`README_DEPLOY.md`** - обзор файлов развертывания
- **`TRANSFER_DATA_GUIDE.md`** - перенос данных с локального компьютера

## ✅ Чеклист перед деплоем

- [ ] Frontend собран локально (`frontend/dist/` существует)
- [ ] `.env.production` создан и заполнен
- [ ] Docker и Docker Compose установлены на сервере
- [ ] Проект склонирован на сервер
- [ ] Nginx установлен (для обратного прокси)
- [ ] Порты 80, 443, 3000, 8080 свободны

## 🎉 Готово!

После выполнения `bash scripts/deploy.sh` ваше приложение будет доступно по адресу вашего сервера!

---

**Вопросы?** См. `DEPLOYMENT_GUIDE.md` для подробных инструкций.



